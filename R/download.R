
# add to mine_links after completed:

  # To download the files, and store them is a handy file structure, use
  # `{cermine}`'s function `download_data()` on the result of `mine_links()`.



download_data <- function() {
  # of class `cermine_data`
}
